@model IEnumerable<MicroShopping.WebUI.Models.ProductCategoryModel>

@{
    ViewBag.Title = "Categories de Productos | MicroShopping";
}

@Html.Partial("_AdminNavigation")

<div class="admin-content">
    <h3>Listado de Categorias de Productos:</h3>

    <a href="@Url.Action("Create","Category")" class="btn btn-info">Crear Categoria</a>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Codigo</th>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var package in Model)
            {
                <tr>
                    <td>@package.ProductCategoryId</td>
                    <td>@package.Name</td>
                    <td>
                        <a class="btn btn-small btn-success" href="@Url.Action("Edit", "Category", new { id = package.ProductCategoryId })">Editar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
